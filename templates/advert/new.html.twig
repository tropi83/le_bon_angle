{% extends 'base.html.twig' %}

{% block title %}Nouvelle publication{% endblock %}

{% block body %}
    <section class="hero is-link">
        <div class="hero-body">
            <a class="button is-primary float-right" href="{{ path('advert_index') }}">
                <span class="icon">
                    <i class="fas fa-long-arrow-alt-left"></i>
                </span>
                <span>Retour aux publications</span>
            </a>
            <p class="title">
                Nouvelle publication
            </p>
            <p class="subtitle">
                Completez les champs suivants:
            </p>
        </div>
    </section>
    <div class="container">
        <div class="section">
            {{ include('advert/_form.html.twig') }}
        </div>
    </div>
{% endblock %}
