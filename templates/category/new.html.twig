{% extends 'base.html.twig' %}

{% block title %}Nouvelle catégorie{% endblock %}

{% block body %}
    <section class="hero is-link">
        <div class="hero-body">
            <a class="button is-primary float-right" href="{{ path('category_index') }}">
                <span class="icon">
                    <i class="fas fa-long-arrow-alt-left"></i>
                </span>
                <span>Retour aux catégories</span>
            </a>
            <p class="title">
                Nouvelle catégorie
            </p>
            <p class="subtitle">
                Completez les champs suivants:
            </p>
        </div>
    </section>
    <div class="container">
        <div class="section">
            {{ include('category/_form.html.twig') }}
        </div>
    </div>

{% endblock %}
