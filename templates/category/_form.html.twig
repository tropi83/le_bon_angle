{{ form_start(form) }}
    <div class="field">
        {{ form_row(form.name) }}
        <div class="has-text-danger">
            {{ form_errors(form.name) }}
        </div>
    </div>
    <div style="text-align: center">
        {{ form_row(form.submit) }}
    </div>
{{ form_end(form) }}